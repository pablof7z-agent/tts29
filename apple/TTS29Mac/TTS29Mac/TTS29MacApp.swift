import SwiftUI
import TTS29Feature

@main
struct TTS29MacApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
        .defaultSize(width: 520, height: 640)
    }
}
